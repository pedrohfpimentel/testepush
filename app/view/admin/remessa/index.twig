{% extends "admin/layout.twig" %}
{% block html_title %}Remessa - {{ parent() }}{% endblock %}
{% set nav_active = 'role' %}
{% block breadcrumbs %}
<ol class="breadcrumb">
  <li><a href="{{ base_url() }}/admin">Início</a></li>
  <li class="active">Remessa</li>
</ol>
{% endblock %}
{% block content %}

<div class="card">
  <div class="card-header" data-background-color="blue">
    <div class="row">
      <div class="col-sm-8">
        <h3>Remessa</h3>
      </div>
      <div class="col-sm-4">
        <p class="pull-right">
          <a class="btn btn-success" href="{{ base_url() }}/admin/remessa/add">
            <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
          </a>
        </p>
      </div>
    </div>
  </div>

  <div class="card-content">
    <div class="row">
      <div class="col-xs-12">
        {% if remessa %}
          <table class="table table-striped data-table">
          <thead>
            <tr>
              <th>Nome</th>
              <th>Descrição</th>
             
              <th class="th-button"></th>
              <th class="th-button"></th>
            </tr>
          </thead>
          <tbody>
            {% for remessa in remessa %}
            <tr>
              <td>{{ remessa.quantity }}</td>
              <td>{{ remessa.type }}</td>
             
              <td style="width:100px;"><a class="btn btn-default" href="{{ base_url() }}/admin/remessa/edit/{{ remessa.id }}">
                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Editar
              </a></td>
              <td style="width:100px;"><a class="btn btn-danger" href="{{ base_url() }}/admin/remessa/remove/{{ remessa.id }}">
                <span class="glyphicon glyphicon-trash" aria-hidden="true"></span> Apagar
              </a></td>
            </tr>
            {% endfor %}
          </tbody>
          </table>
        {% else %}
          <div class="alert alert-danger" role="alert">Não existem dados</div>
        {% endif %}
      </div>
    </div>
  </div>
</div>



{% endblock %}
